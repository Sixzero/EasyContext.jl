const highlight_changes_guide = """
Always use the "... existing code ..." if you skip or leave out codepart! 
""" 
const highlight_changes_guide_v2 = """
Use "... existing code ..." to indicate skipped/unchanged code sections.
Inside code where a comment prefix is needed, use "// ... existing code ..." (or the language's comment syntax).

Focus on the changes only — write what you're modifying and abbreviate the rest.
""" 