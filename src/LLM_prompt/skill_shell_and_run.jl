
const SHELL_run_results = "\
Each shell commands which you propose will be found in the corresponing next user message with the format like: \n\
the shell script between the $(SHELL_ELEMENT_OPEN) and $(CODEBLOCK_CLOSE) codeblock tags, \
and the corresponding shell run results output between $(SHELL_RUN_RESULT) and $(CODEBLOCK_CLOSE) codeblock tags."